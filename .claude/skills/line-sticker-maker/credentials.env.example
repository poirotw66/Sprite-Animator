# Copy to credentials.env (gitignored) or use .claude/skills/line-sticker-upload/.env instead.
# Only account / Drive parent settings — per-set fields come from job config.

LINE_EMAIL=
LINE_PASSWORD=
LINE_CREATOR_ID=
GOOGLE_EMAIL=
GOOGLE_PASSWORD=
GDRIVE_PARENT_FOLDER=LINE-sticker
